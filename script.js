const header=document.querySelector('.site-header'),menu=document.querySelector('.menu-button'),lightbox=document.querySelector('.lightbox'),upload=document.querySelector('.upload-dialog'),grid=document.querySelector('#art-grid'),form=document.querySelector('#upload-form'),input=form.elements.image,preview=document.querySelector('#image-preview');let db;const request=indexedDB.open('studio-masengesho',1);request.onupgradeneeded=e=>e.target.result.createObjectStore('drawings',{keyPath:'id',autoIncrement:true});request.onsuccess=e=>{db=e.target.result;const read=db.transaction('drawings').objectStore('drawings').getAll();read.onsuccess=()=>read.result.forEach(addCard)};menu.onclick=()=>{const open=header.classList.toggle('open');menu.setAttribute('aria-expanded',open)};function show(card){const copy=card.querySelector('.artwork').cloneNode(true);lightbox.querySelector('.lightbox-art').replaceChildren(copy);lightbox.querySelector('h2').textContent=card.dataset.title;lightbox.querySelector('.medium').textContent=card.dataset.medium;lightbox.showModal()}function bind(card){card.onclick=()=>show(card)}function addCard(d){const card=document.createElement('button');card.className='art-card uploaded-art';card.dataset.title=d.title;card.dataset.medium=d.medium;card.innerHTML='<span class="artwork"><img alt=""></span><span class="art-caption"></span>';card.querySelector('img').src=d.image;card.querySelector('img').alt=d.title;card.querySelector('.art-caption').append(d.title,Object.assign(document.createElement('small'),{textContent:String(document.querySelectorAll('.art-card').length+1).padStart(2,'0')}));bind(card);grid.append(card)}document.querySelectorAll('.art-card').forEach(bind);document.querySelector('.upload-trigger').onclick=()=>upload.showModal();document.querySelector('.upload-close').onclick=()=>upload.close();lightbox.querySelector('.close').onclick=()=>lightbox.close();input.onchange=()=>{const file=input.files[0];if(file){preview.src=URL.createObjectURL(file);preview.hidden=false}};form.onsubmit=e=>{e.preventDefault();const file=input.files[0];if(!file||!db)return;const reader=new FileReader();reader.onload=()=>{const drawing={title:form.elements.title.value,medium:form.elements.medium.value,image:reader.result};const save=db.transaction('drawings','readwrite').objectStore('drawings').add(drawing);save.onsuccess=()=>{addCard(drawing);form.reset();preview.hidden=true;upload.close()}};reader.readAsDataURL(file)};[lightbox,upload].forEach(d=>d.addEventListener('click',e=>{if(e.target===d)d.close()}));document.querySelectorAll('nav a').forEach(a=>a.onclick=()=>header.classList.remove('open'));
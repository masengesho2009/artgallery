const header=document.querySelector('.site-header'),menuButton=document.querySelector('.menu-button'),dialog=document.querySelector('.lightbox'),closeButton=dialog.querySelector('.close');menuButton.addEventListener('click',()=>{const open=header.classList.toggle('open');menuButton.setAttribute('aria-expanded',open)});document.querySelectorAll('.art-card').forEach(card=>card.addEventListener('click',()=>{const preview=card.querySelector('.artwork').cloneNode(true),art=dialog.querySelector('.lightbox-art');art.replaceChildren(preview);preview.style.height='100%';dialog.querySelector('h2').textContent=card.dataset.title;dialog.querySelector('.medium').textContent=card.dataset.medium;dialog.showModal()}));closeButton.addEventListener('click',()=>dialog.close());dialog.addEventListener('click',event=>{if(event.target===dialog)dialog.close()});document.querySelectorAll('nav a').forEach(link=>link.addEventListener('click',()=>header.classList.remove('open')));

